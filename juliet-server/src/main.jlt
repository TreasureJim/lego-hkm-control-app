using .RobotServer
using .AppServer
using Avon

function main()
	# var serverinfo = robot_start_server(UInt16(9000))
	# var enc = serverinfo.enc
	# robot_stop_server(serverinfo)

	for i = 0:7
		println(i)
	end

end
